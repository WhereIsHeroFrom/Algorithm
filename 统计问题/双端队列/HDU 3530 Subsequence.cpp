#include <iostream>
#include <cstring>
#include <cstdlib>

using namespace std;

#define maxn 100010

int a[maxn];

int Max
int main() {
	int n, m, k;
	int i;

	while(scanf( "%d", &n ) != EOF) {
		scanf("%d %d", &m, &k);
		for(i = 1; i <= n; ++i) {
			scanf("%d", &a[i]);
		}
		int l, r;
		l = r = 1;
		while(1) {
			 
			
		} 
	}
	return 0;
}
